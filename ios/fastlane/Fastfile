# Uncomment the line if you want fastlane to automatically update itself
# update_fastlane

default_platform(:ios)

platform :ios do
  desc "Builds the ipa file"
  lane :beta do
     match(type: "development", readonly: true, keychain_password: ENV["MATCH_PASSWORD"])
     gym
  end
end
