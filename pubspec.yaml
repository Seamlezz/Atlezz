name: Atlezz
description: The guide to infinite wisdom

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.3
  # Functional
  provider: ^4.3.1
  # Firebase
  firebase_analytics: ^5.0.16
  firebase_crashlytics: ^0.1.4+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  effective_dart: ^1.2.4

flutter:
  uses-material-design: true